# ===== Database (Postgres) =====
# Exemplo local:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/media_repository?schema=public"
#
# Supabase (Vercel / serverless) - recomendado usar o POOLER (PgBouncer):
# - Copie do painel do Supabase em Settings -> Database -> Connection string -> "Connection pooling"
# - Se sua senha tiver caracteres especiais (ex: @, $), faça URL-encode (%40, %24)
# Exemplo (substitua pelos seus valores):
# DATABASE_URL="postgresql://postgres.<project-ref>:<password>@<pooler-host>:6543/postgres?pgbouncer=true&connection_limit=1&sslmode=require"
#
# Supabase (DIRECT, usado por migrations/Prisma Studio) - conexão direta no :5432:
DATABASE_URL=""

# ===== Clerk (Auth) =====
# Pegue em: `https://dashboard.clerk.com/` (API Keys)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
CLERK_SECRET_KEY=""

# (Opcional) Rotas padrão do Clerk
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL="/"
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL="/"

# ===== Proteção de conteúdo arquivado =====
# Usado em /api/medias/archived
ARCHIVED_SECRET=""

# ===== CORS para /api (Client consumindo Admin) =====
# Por padrão o `admin/next.config.mjs` libera http://localhost:3001
ACCESS_CONTROL_ALLOW_ORIGIN="http://localhost:3001"
ACCESS_CONTROL_ALLOW_CREDENTIALS="true"
ACCESS_CONTROL_ALLOW_METHODS="GET,DELETE,PATCH,POST,PUT"
ACCESS_CONTROL_ALLOW_HEADERS="X-CSRF-Token, X-Requested-With, Accept, Accept-Version, Content-Length, Content-MD5, Content-Type, Date, X-Api-Version"

